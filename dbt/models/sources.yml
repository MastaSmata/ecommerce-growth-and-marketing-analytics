version: 2

sources:
  - name: raw
    database: "{{ env_var('BIGQUERY_PROJECT') }}"
    schema: "{{ env_var('BIGQUERY_DATASET') }}"

    tables:
      - name: raw_campaigns
      - name: raw_customers
      - name: raw_products
      - name: raw_sales
      - name: raw_channels
      - name: raw_marketing